apiVersion: v1
kind: Service
metadata:
  name: mcpo-postgres
spec:
  selector:
    app: mcpo-postgres
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: LoadBalancer
